# Phase 4 mixing/separation/release dependencies (scripts/separate.py, mix.py, make_subs.py, make_release.py)
#
# Note: install these in a separate env (e.g. conda phase4, python 3.11), not the
# sovits env — audio-separator needs librosa>=0.10, which conflicts with the
# librosa==0.9.1 pinned by so-vits-svc.
# For setting up the sovits inference environment, see docs/phase3_setup_guide.md
# (not managed by this file).
# make_subs.py / make_release.py only need the standard library + system ffmpeg.
#
#   conda create -n phase4 python=3.11 -y
#   conda activate phase4
#   pip install -r requirements.txt

audio-separator[gpu]
pedalboard
pyloudnorm
soundfile
numpy
resampy

# Automatic subtitle timing (scripts/auto_line_times.py).
# Note: this env's torch is built for CUDA 13, while ctranslate2 needs CUDA 12's
# cublas/cudnn. To run on GPU: point LD_LIBRARY_PATH at the sovits env's
# nvidia/{cublas,cudnn}/lib, or pip install nvidia-cublas-cu12 nvidia-cudnn-cu12;
# when the libs are missing the script falls back to CPU automatically.
faster-whisper
pykakasi
